import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Ayyan Khan — Graphic Designer',
  description: 'Ayyan Khan is a graphic designer specializing in branding, visual identity, logo design, social media design and creative visual communication.',
  openGraph: { title: 'Ayyan Khan — Graphic Designer', description: 'Bold, purposeful visual identities, digital graphics and creative communication.', type: 'website' },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
